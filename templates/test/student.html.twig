{% extends 'base.html.twig' %}

{% block title %}{{title}}{% endblock %}

{% block body %}

    <h1>{{ title }}</h1>
    <h2>{{ student.firstName }} {{ student.LastName }} (id {{ tag.id }})<h2>
    <p>
        {{ tag.description }}
    </p>

{# {% for tag in tags %}
    <h2>{{ tag.name }} (id {{ tag.id }})<h2>
    <p>
        {{ tag.description }}
    </p>
{% endfor %} #}

{% endblock %}
