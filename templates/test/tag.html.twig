{% extends 'base.html.twig' %}

{% block title %}{{title}}{% endblock %}

{% block body %}

<h2>Liste des tags sans description</h2>

{% for tag in nullDescriptionTags %}
    <h2>{{ tag.name }} (id {{ tag.id }})<h2>
    <p>
        {{ tag.description }}

    </p>
{% endfor %}

<h2>Liste des tags avec description</h2>

{% for tag in notNullDescriptionTags %}
    <h3>{{ tag.name }} (id {{ tag.id }})<h3>
    <p>
        {{ tag.description }}

    </p>
{% endfor %}

<h2>Liste des tags avec mot-clé</h2>

{% for tag in keywordsTags1 %}
    <h3>{{ tag.name }} (id {{ tag.id }})<h3>
    <p>
        {{ tag.description }}

    </p>
{% endfor %}

{% for tag in keywordsTags2 %}
    <h3>{{ tag.name }} (id {{ tag.id }})<h3>
    <p>
        {{ tag.description }}

    </p>
{% endfor %}


<h2>Liste des tags d'une school year</h2>

{% for tag in schoolYearTags %}
    <h3>{{ tag.name }} (id {{ tag.id }})<h3>
    <p>
        {{ tag.description }}

    </p>
{% endfor %}

<h2>Liste des students du tag 4 (python) </h2>

<ul>
{% for student in tag4.students %}
    <li>{{ student.firstName}} {{ student.lastName}} {{ student.id}}</li>
{% endfor %}
</ul>

{% endblock %}
